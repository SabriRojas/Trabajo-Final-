var nombreVariable;
var nombre_variable;
var nombrevariable;
var NombreVariable; 

/* PARA DECLARA UNA VARIABLE TENER EN CUENTA
    - No debe iniciar con un número
    - No debe tener espacios de por medio
    - Darle un nombre descriptivo a la variable
    - Utilizar lowerCameCase
*/

var nombreVariable;
var apodo = "Nolo";
let edad = 22; 

let variablelet;
const variableConstante = 0;


var edad; // declarando solamente

edad = 22; // una asignacion

var edadAnterior = edad; // declaración con asignacion directa 


// Tipos de variables, las principales son:

// Variable de tipo de texto
var textoComilladDoble = "Hola mundi!";
var textoComillasSimple = 'A';
var texto = "";

// Variable de tipo numerico 
var numerico = 0;
var promedio = 7;
var pi = 3.14151707459845;
var peso = 60.570;
var negativo = -350;
var negativoPreciso = -7.9384539875;
var negativosNoTanPreciso = -4.123;

// Variable tipo booleano o (verdadero o falso)
var booleano = true;
var booleanoFalso = false;
var booleanoVerdadero = true;

var array // corchetes
var objeto ()


//Dif entre LET Y VAR
var variableVar; // var = scope o llegada global

let variableLet; // let = scope o llegada local 

fuction beber (){
    let bebida = "agua";

    console.log (bebida);
}

// Distintos tipos de operacion
var numero1 = 40; 
var numero2 = 60;
var resultado;

resultado = numero1 + numero2;

// alert(resultado);
// console.log(resultados);

//Nan = Not a Number = NO es un valor numerico


var decremento = 1

//EJ1
console.log(decremento);
decremento--
console.log(decremento++);

//EJ2
console.log(numero);
console.log(-numero);


//OPERADORES LOGICOS Y RELACIONALES

var variable1 ="2";

var variable2 = 2;

var resultado;

resultado = variable1 == variable2

console.log(resultado);

//ambas variables tienen el mismo valor pero su tipo es disntinto (texto y numerico) y dan el mismo valor = 2

// == (dos operadores) se compara valor y son iguales aunque sean de disntitos tipos y me va dar TRUE

// !== estrictamente desigual porque son de disntitno tipo de variante me vdar FALSE

// === es estrictamente igual 

/*
    && = a  y b tienen que ser verdaderos (and)
    II = a o b tienen que ser verdaderos (or)
    ! = tiene que ser el opuesto a verdadero (negado)
*/

// EJEMPLO

var variable1 = 1;
var variable2 = 2;
var variable2 = 3;

var resultado;

resultado = variable1 <== variable2 && <== variable3;

console.log(resultado);





